extends layout

block content

    include errors

    form(action="/books" + book.id + "?method=PUT", method="post")
    p
        label(for="title") Title
        input(name="title", id="title", type="text", value=book.title)
    p
        label(for="author") Author
        input(name="author", id="author", type="text", value=book.author)
    p
        label(for="genre") Genre
        input(name="genre", id="genre", type="text", value=book.genre)
    p
        label(for="year") Year
        input(name="year", id="year", type="text", value=book.year)
    p
        input(type="submit", value="Update Book")

  form(action="/books" + book.id +"?_method=DELETE", method="post", onsubmit="return confirm('Warning: Are you sure you want to permanently delet this book?');")
    p
        a.button(href="/") Cancel  
      //- a.(href="/books", class="button") Cancel
    p   
        input(type="submit", value="Delete Book")